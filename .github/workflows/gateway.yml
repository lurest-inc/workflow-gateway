name: Lurest Workflow Gateway

on:
  workflow_call:
    inputs:
      workflow_file:
        description: "private-workflows 側の workflow ファイル名（例: core.yml）"
        required: false
        default: "core.yml"
        type: string
      ref:
        description: "private-workflows 側の起動対象 ref（例: main）"
        required: false
        default: "main"
        type: string
      inputs_json:
        description: 'workflow_dispatch の inputs を JSON 文字列で渡す（例: {"foo":"bar"}）'
        required: false
        default: "{}"
        type: string
    secrets:
      LUREST_DISPATCH_TOKEN:
        description: "Lurest が発行した fine-grained PAT（private-workflows に対する Actions: write が必要）"
        required: true

jobs:
  dispatch:
    runs-on: ubuntu-latest

    steps:
      - name: Validate inputs
        shell: bash
        run: |
          set -euo pipefail

          WORKFLOW_FILE="${{ inputs.workflow_file }}"

          # ざっくり危険な指定を弾く（パスっぽい指定や変な文字）
          if [[ ! "$WORKFLOW_FILE" =~ ^[A-Za-z0-9_.-]+\.ya?ml$ ]]; then
            echo "❌ Invalid workflow_file: $WORKFLOW_FILE"
            exit 1
          fi

          # ✅ ここは「許可リスト」運用にするのが安全です（必要に応じて増やす）
          ALLOWLIST=("core.yml")
          allowed=false
          for f in "${ALLOWLIST[@]}"; do
            if [[ "$WORKFLOW_FILE" == "$f" ]]; then allowed=true; fi
          done

          if [[ "$allowed" != "true" ]]; then
            echo "❌ workflow_file is not allowed: $WORKFLOW_FILE"
            echo "Allowed: ${ALLOWLIST[*]}"
            exit 1
          fi

      - name: Dispatch private workflow (workflow_dispatch)
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.LUREST_DISPATCH_TOKEN }}
          TARGET_REPO: lurest-inc/private-workflows
          WORKFLOW_FILE: ${{ inputs.workflow_file }}
          TARGET_REF: ${{ inputs.ref }}
          INPUTS_JSON: ${{ inputs.inputs_json }}
        run: |
          set -euo pipefail

          # inputs_json が壊れていたら止める
          echo "$INPUTS_JSON" | python -c 'import json,sys; json.load(sys.stdin)'

          # payload を生成（jq で JSON 構築）
          payload=$(jq -n \
            --arg ref "$TARGET_REF" \
            --arg inputs_json "$INPUTS_JSON" \
            '{ref: $ref, inputs: ($inputs_json | fromjson)}')

          echo "🚀 Dispatching ${TARGET_REPO} / ${WORKFLOW_FILE} @ ${TARGET_REF}"
          code=$(curl -L -sS -o /dev/null -w "%{http_code}" \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${TARGET_REPO}/actions/workflows/${WORKFLOW_FILE}/dispatches" \
            -d "${payload}")

          if [[ "$code" != "204" ]]; then
            echo "❌ Dispatch failed. HTTP ${code}"
            exit 1
          fi

          echo "✅ Dispatch accepted (HTTP 204)."

      - name: Summary
        run: |
          echo "### ✅ Lurest Workflow Gateway" >> $GITHUB_STEP_SUMMARY
          echo "- Dispatched: lurest-inc/private-workflows" >> $GITHUB_STEP_SUMMARY
          echo "- Workflow: \`${{ inputs.workflow_file }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Ref: \`${{ inputs.ref }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Note: dispatch は非同期です（private 側の完了待ちはしません）" >> $GITHUB_STEP_SUMMARY
